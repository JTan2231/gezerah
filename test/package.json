{
  "name": "dnd-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "ci": "bun install --frozen-lockfile",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "tsc --noEmit",
    "test:scenarios": "bun test src/scenario/architecture-tests",
    "verify:scenarios": "bun src/scenario/verify.ts",
    "e2e": "tsx src/e2e.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.0.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
